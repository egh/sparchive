language: python
python:
  - "2.7"
  - "3.2"
before_install:
 - sudo apt-get update -qq
 - sudo apt-get install -qq rzip
# command to install dependencies
# install: "pip install -r requirements.txt"
# command to run tests
script: nosetests
